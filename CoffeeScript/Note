The download thing for coffeescript is:
npm install --save-dev coffeescript
npm -g install coffee-script

Before you can download coffee with npm, run:
sudo apt-get install npm

After coffee install run:
coffee

Result:
coffee>

if run with file, run:
coffee something.coffee
